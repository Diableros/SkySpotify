export type ThemeType = {
  color: {
    textMain: string
    textSecondry: string
    hover: string
    active: string
    bgMain: string
    bgMenu: string
    bgDropList: string
    bgDropListSlider: string
    bgTrackHover: string
  }
}
